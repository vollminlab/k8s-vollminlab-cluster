# ---------------------------------------------------------------------------
# Import blocks for pre-existing Cloudflare resources
#
# All DNS records were imported in earlier PRs and are already in TF state.
# All tunnel resources are now TF-created from scratch — no tunnel imports.
# No active import blocks needed.
# ---------------------------------------------------------------------------

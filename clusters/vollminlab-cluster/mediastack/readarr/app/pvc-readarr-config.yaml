apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-readarr-config
  namespace: mediastack
  labels:
    app: readarr
    env: production
    category: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

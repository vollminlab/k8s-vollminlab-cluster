apiVersion: v1
kind: ConfigMap
metadata:
  name: portainer-values
  namespace: portainer
  labels:
    app: portainer
    env: production
    category: apps
data:
  values.yaml: |
    service:
      type: ClusterIP
      annotations: {}
    ingress:
      enabled: false
    persistence:
      enabled: true
      # Declared in pvc.yaml rather than left to the chart, so the claim is
      # GitOps-managed -- the pre-#1101 one existed only in the cluster, in no
      # manifest, and nothing would have recreated it.
      existingClaim: portainer
    resources:
      limits:
        cpu: 100m
        memory: 128Mi
      requests:
        cpu: 100m
        memory: 128Mi
    serviceAccount:
      create: true
      name: portainer-sa-clusteradmin
      annotations: {}
    rbac:
      enabled: true
    edge:
      enabled: true
      tunnelPort: 30776
    securityContext:
      runAsNonRoot: false
      runAsUser: 0

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: b2-exporter
  labels:
    app: b2-exporter
    env: production
    category: observability
resources:
  - deployment.yaml
  - service.yaml
  - servicemonitor.yaml
  - b2-exporter-credentials-externalsecret.yaml

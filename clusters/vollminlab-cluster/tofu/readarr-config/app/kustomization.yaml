apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: readarr-config
  labels:
    app: tofu-controller
    env: production
    category: core
resources:
  - readarr-tf-credentials-sealedsecret.yaml
  - terraform-cr.yaml

---
apiVersion: infra.contrib.fluxcd.io/v1alpha2
kind: Terraform
metadata:
  name: cloudflare-config
  namespace: tofu
  labels:
    app: tofu-controller
    env: production
    category: core
spec:
  interval: 10m
  # TEMPORARY -- held on manual to review #1232 cloudflare 5.24.x -> 5.25.0.
  # Under `auto` the bump PR applies within the 10m interval with no plan read.
  # Restored to `auto` by the follow-up PR; see docs/runbooks/tofu-provider-bumps.md.
  approvePlan: ""
  path: ./terraform/cloudflare
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  backendConfig:
    customConfiguration: |
      backend "s3" {
        bucket                      = "terraform-state"
        key                         = "cloudflare/terraform.tfstate"
        region                      = "us-east-1"
        endpoint                    = "http://minio.minio.svc.cluster.local:9000"
        force_path_style            = true
        skip_credentials_validation = true
        skip_metadata_api_check     = true
        skip_region_validation      = true
      }
  backendConfigsFrom:
    - kind: Secret
      name: tofu-minio-credentials
  varsFrom:
    - kind: Secret
      name: cloudflare-tf-credentials
  writeOutputsToSecret:
    name: cloudflare-tunnel-ids
